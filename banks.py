class Bank:

    def __init__(self, name, phone, card_start, cards_num, sum_start, sum_end, minus, plus, balance, b_end, currency):
        self.name = name
        self.phone = phone
        self.card_start = card_start
        self.cards_num = cards_num
        self.sum_start = sum_start
        self.sum_end = sum_end
        self.minus = minus
        self.plus = plus
        self.balance = balance
        self.b_end = b_end
        self.currency = currency
